# sion(Python AGV) 환경 변수 예시
# 실 사용 시 이 파일을 .env로 복사 후 값을 채우거나, 셸에서 export해 사용한다.
# 코드에서는 os.environ으로 직접 읽으며, 별도 dotenv 자동 로딩은 하지 않는다.

# AGV ↔ sion-backend WebSocket 엔드포인트
# 미설정 시 기본값: ws://localhost:3000/websocket/agv
SION_BACKEND_WS_URL=ws://localhost:3000/websocket/agv
