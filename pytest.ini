[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers
markers =
    unit: 순수 함수 단위 테스트 (외부 의존 없음)
    integration: 여러 모듈을 함께 검증
    slow: 1초 이상 걸리는 테스트
